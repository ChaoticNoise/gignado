<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapsible" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Gignado", gigs_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapsible">
      <%= render_navigation renderer: :bootstrap_renderer %>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to edit_member_path(current_member) do %>
            <%= image_tag(current_member.image_url, class: 'img-circle', id: 'member-img') %>
            <%= current_member.display_name %>
          <% end %>
        </li>
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>
